// It doesn't really matter if this is a .js or .ts file, it will be compiled to .js anyway
// And it doesn't seem like TS works quite well...
import module from "../../crate/Cargo.toml";
module.run();
